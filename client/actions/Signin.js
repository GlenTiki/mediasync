import { createAction } from 'redux-actions'

export const landingHandleSelect = createAction('LANDING_HANDLE_SELECT')
export const navHandleSelect = createAction('NAV_HANDLE_SELECT')
export const landingHandleError = createAction('LANDING_HANDLE_ERROR')
export const navHandleError = createAction('NAV_HANDLE_ERROR')
